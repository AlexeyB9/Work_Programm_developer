# 🔧 Решение проблем с Docker

## Сообщение "guap is up-to-date"

Это означает что контейнер уже запущен. Проверьте:

```bash
# Статус контейнера
docker ps

# Логи
docker-compose logs -f guap
```

## Если контейнер не отвечает:

### 1. Перезапустите контейнер:
```bash
docker-compose restart
```

### 2. Пересоздайте контейнер:
```bash
docker-compose down
docker-compose up -d
```

### 3. Пересоберите образ:
```bash
docker-compose build --no-cache
docker-compose up -d
```

## Проверка переменных окружения:

```bash
# Проверьте что переменные установлены
docker exec guap env | grep -E "PPLX|TELEGRAM|PORT"
```

Если переменных нет, создайте `.env` файл или укажите их в `docker-compose.yml`

## Проверка портов:

```bash
# Проверьте что порт 8000 открыт
netstat -tuln | grep 8000
# или
ss -tuln | grep 8000
```

## Проверка файла шаблона:

```bash
# Проверьте что шаблон существует
docker exec guap ls -la /app/files/Шаблон.docx
```

## Просмотр всех логов:

```bash
# Последние 100 строк
docker-compose logs --tail=100 guap

# В реальном времени
docker-compose logs -f guap

# Все логи с начала
docker-compose logs guap
```

## Вход в контейнер для отладки:

```bash
docker exec -it guap /bin/bash

# Внутри контейнера:
python run_all.py  # Запуск вручную для проверки
```

## Очистка и перезапуск:

```bash
# Остановить и удалить контейнер
docker-compose down

# Удалить образ (опционально)
docker rmi work_programm_developer_app

# Пересобрать и запустить
docker-compose build --no-cache
docker-compose up -d
```

## Типичные ошибки:

### Ошибка: "PPLX_API_KEY не установлен"
**Решение:** Создайте `.env` файл или укажите переменные в `docker-compose.yml`

### Ошибка: "Port 8000 is already allocated"
**Решение:** Измените порт в `docker-compose.yml` на другой (например, 8001)

### Ошибка: "ModuleNotFoundError"
**Решение:** Пересоберите образ: `docker-compose build --no-cache`

### Контейнер падает сразу после запуска
**Решение:** Проверьте логи: `docker-compose logs guap`

