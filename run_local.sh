#!/bin/bash
# Скрипт запуска проекта локально (Linux/Mac)

echo "💻 Запуск проекта локально..."
echo ""

# Проверка виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

echo "🔌 Активация виртуального окружения..."
source venv/bin/activate

# Проверка зависимостей
if ! python -c "import fastapi" 2>/dev/null; then
    echo "📥 Установка зависимостей..."
    pip install -r requirements.txt
fi

# Проверка переменных окружения
if [ -z "$PPLX_API_KEY" ]; then
    echo ""
    echo "⚠️  ВНИМАНИЕ: PPLX_API_KEY не установлен!"
    echo "   Установите: export PPLX_API_KEY=ваш_ключ"
    echo ""
fi

# Проверка шаблона
if [ ! -f "files/Шаблон.docx" ]; then
    echo "❌ ОШИБКА: files/Шаблон.docx не найден!"
    exit 1
fi

echo ""
echo "🚀 Запуск всех сервисов..."
echo "   Веб-интерфейс: http://localhost:8000"
echo "   Для остановки нажмите Ctrl+C"
echo ""

python run_all.py

