# 🔧 Исправление ошибки ContainerConfig

## Проблема
```
KeyError: 'ContainerConfig'
```

Эта ошибка возникает когда docker-compose пытается прочитать конфигурацию старого поврежденного контейнера.

## Решение

### Вариант 1: Полная очистка (рекомендуется)

```bash
# 1. Остановить и удалить все контейнеры
docker-compose down
docker stop guap 2>/dev/null || true
docker rm guap 2>/dev/null || true

# 2. Удалить образы
docker rmi work_programm_developer_app 2>/dev/null || true
docker rmi guap-app 2>/dev/null || true

# 3. Очистить неиспользуемые ресурсы
docker container prune -f
docker image prune -f

# 4. Пересобрать и запустить
docker-compose build --no-cache
docker-compose up -d
```

### Вариант 2: Использовать скрипт очистки

```bash
chmod +x cleanup.sh
./cleanup.sh
docker-compose build --no-cache
docker-compose up -d
```

### Вариант 3: Полная очистка Docker (если ничего не помогает)

```bash
# ОСТОРОЖНО: Удалит ВСЕ остановленные контейнеры, образы, сети
docker system prune -a -f

# Затем пересоберите
docker-compose build --no-cache
docker-compose up -d
```

## Проверка после исправления

```bash
# Проверка статуса
docker ps

# Проверка логов
docker-compose logs -f guap

# Проверка доступности
curl http://localhost:8000
```

## Предотвращение проблемы в будущем

Всегда используйте:
```bash
docker-compose down  # перед остановкой
docker-compose up -d # для запуска
```

Не используйте `docker stop` напрямую, если работаете с docker-compose.

