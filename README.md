# ГУАП - Формирование учебной программы

Веб-приложение и Telegram бот для автоматического формирования учебной программы на основе шаблона и учебных материалов через Perplexity API.

## 🚀 Быстрый старт

1. Установите зависимости: `pip install -r requirements.txt`
2. Убедитесь что файл `files/Шаблон.docx` присутствует
3. Запустите все сервисы: `python run_all.py`
4. Откройте http://localhost:8000 в браузере

## 📦 Развертывание на сервере

**Быстрый старт:** См. [docs/QUICK_START.md](docs/QUICK_START.md)  
**Подробная инструкция:** См. [docs/DEPLOY.md](docs/DEPLOY.md)  
**Чеклист перед развертыванием:** См. [docs/CHECKLIST.md](docs/CHECKLIST.md)

**Рекомендуемые бесплатные хостинги:**
- 🚂 **Railway** ⭐ - лучший вариант, 500 часов бесплатно в месяц, автоматическое развертывание
- 🎨 **Render** - простой и удобный, но может "засыпать" после 15 минут неактивности
- 🐍 **PythonAnywhere** - для экспериментов, бесплатный тариф с ограничениями
- ☁️ **VPS (Vultr/DigitalOcean)** - полный контроль, но требует настройки

## Структура проекта

```
Work_Programm_developer/
├── api.py              # FastAPI веб-сервер
├── run_all.py          # Запуск всех сервисов
├── main.py             # CLI версия (опционально)
├── requirements.txt    # Зависимости Python
├── Procfile            # Для Railway/Heroku
├── railway.json        # Конфигурация Railway
├── .gitignore          # Игнорируемые файлы
├── wpd/                # Модули ядра приложения
│   ├── init_core.py
│   ├── request_api.py
│   ├── merge_with_docx.py
│   ├── fill_tables.py
│   └── ...
├── tgbot/              # Telegram бот
│   └── bot.py
├── files/              # Рабочие файлы
│   └── Шаблон.docx    # Шаблон (обязателен!)
├── config/             # Конфигурационные файлы
│   ├── Dockerfile
│   ├── runtime.txt
│   └── env.example
├── scripts/            # Утилиты для разработки
│   ├── cleanup.py
│   └── prepare_deploy.*
└── docs/               # Документация
    ├── QUICK_START.md
    ├── DEPLOY.md
    └── CHECKLIST.md
```

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что файл шаблона находится в `files/Шаблон.docx`

## Запуск всех сервисов (рекомендуется)

Для запуска веб-сервера и Telegram бота одновременно:

```bash
python run_all.py
```

Или используйте скрипты:
```bash
# Windows
run_all.bat

# Linux/Mac
chmod +x run_all.sh
./run_all.sh
```

После запуска:
- Веб-интерфейс: http://localhost:8000
- Telegram бот будет работать автоматически

## Запуск веб-приложения отдельно

```bash
python api.py
```

Или через uvicorn:
```bash
uvicorn api:app --host 0.0.0.0 --port 8000
```

После запуска откройте в браузере: http://localhost:8000

## Использование

1. Откройте веб-интерфейс в браузере
2. Нажмите "Загрузить учебник" и выберите файл .docx
3. Нажмите "Отправить"
4. Дождитесь обработки (может занять несколько минут)
5. Нажмите "Скачать результат" для получения готового файла

## API Endpoints

- `GET /` - Главная страница с формой загрузки
- `POST /upload` - Загрузка файла и обработка
- `GET /download/{file_id}` - Скачивание результата

## Запуск Telegram бота

1. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram
2. Установите переменную окружения или укажите токен в коде:
```bash
# Windows
set TELEGRAM_BOT_TOKEN=ваш_токен_бота

# Linux/Mac
export TELEGRAM_BOT_TOKEN=ваш_токен_бота
```

3. Запустите бота:
```bash
cd tgbot
python bot.py
```

Или используйте скрипты:
```bash
# Windows
cd tgbot
run_bot.bat

# Linux/Mac
cd tgbot
chmod +x run_bot.sh
./run_bot.sh
```

4. Найдите вашего бота в Telegram и отправьте ему файл учебника в формате .docx

## CLI версия (старая)

Для использования через командную строку:
```bash
python main.py файл1.docx файл2.docx
```

