# 🔧 Исправление ошибки: Couldn't find env file

## Проблема
```
ERROR: Couldn't find env file: /Work_Programm_developer/.env
```

## Решение (выберите один вариант):

### Вариант 1: Создать файл .env (рекомендуется)

```bash
# Перейдите в папку проекта
cd /Work_Programm_developer

# Создайте файл .env
nano .env
```

Добавьте в файл:
```env
PPLX_API_KEY=ваш_ключ_perplexity_api
TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
PORT=8000
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

Затем запустите:
```bash
docker-compose up -d
```

### Вариант 2: Использовать переменные окружения напрямую

Отредактируйте `docker-compose.yml`:

```yaml
services:
  app:
    # Закомментируйте env_file:
    # env_file:
    #   - .env
    
    # Раскомментируйте и укажите переменные:
    environment:
      - PPLX_API_KEY=ваш_ключ_здесь
      - TELEGRAM_BOT_TOKEN=ваш_токен_здесь
      - PORT=8000
```

### Вариант 3: Использовать переменные окружения системы

```bash
# Установите переменные окружения
export PPLX_API_KEY=ваш_ключ
export TELEGRAM_BOT_TOKEN=ваш_токен
export PORT=8000

# Запустите без --env-file
docker-compose up -d
```

### Вариант 4: Автоматическое создание .env

```bash
chmod +x create_env.sh
./create_env.sh
nano .env  # Отредактируйте и укажите ваши ключи
docker-compose up -d
```

## Быстрая команда для создания .env:

```bash
cat > .env << 'EOF'
PPLX_API_KEY=ваш_ключ_perplexity_api
TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
PORT=8000
EOF
```

Затем отредактируйте файл и укажите реальные значения:
```bash
nano .env
```

## Проверка:

```bash
# Проверьте что файл создан
ls -la .env

# Проверьте содержимое (безопасно, если нет секретов)
cat .env
```

## Важно:

- Файл `.env` должен быть в той же папке, где находится `docker-compose.yml`
- Не коммитьте `.env` в git (он уже в `.gitignore`)
- После создания `.env` запустите: `docker-compose up -d`

