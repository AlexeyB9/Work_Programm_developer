# 💻 Запуск программы локально (без Docker)

## Быстрый старт

### 1. Установите зависимости:

```bash
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройте переменные окружения:

**Windows:**
```cmd
set PPLX_API_KEY=ваш_ключ_perplexity_api
set TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
```

**Linux/Mac:**
```bash
export PPLX_API_KEY=ваш_ключ_perplexity_api
export TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
```

**Или создайте файл `.env`** (требуется `python-dotenv`):
```bash
pip install python-dotenv
```

Создайте `.env`:
```env
PPLX_API_KEY=ваш_ключ_perplexity_api
TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
PORT=8000
```

### 3. Убедитесь что шаблон на месте:

Проверьте что файл `files/Шаблон.docx` существует.

### 4. Запустите:

```bash
# Запуск всех сервисов (веб-сервер + Telegram бот)
python run_all.py

# Или только веб-сервер
python api.py

# Или только Telegram бот
cd tgbot
python bot.py
```

### 5. Откройте в браузере:

- Веб-интерфейс: http://localhost:8000
- Telegram бот будет работать автоматически (если токен указан)

## Управление

- Остановка: `Ctrl+C` в терминале
- Перезапуск: просто запустите снова

## Структура для локального запуска:

```
Work_Programm_developer/
├── api.py              # Веб-сервер
├── run_all.py          # Запуск всех сервисов
├── requirements.txt    # Зависимости
├── files/
│   └── Шаблон.docx    # Шаблон (обязателен!)
├── wpd/               # Модули ядра
└── tgbot/             # Telegram бот
```

## Решение проблем локально:

### Ошибка: "PPLX_API_KEY не установлен"
**Решение:** Установите переменную окружения (см. шаг 2)

### Ошибка: "ModuleNotFoundError"
**Решение:** Установите зависимости: `pip install -r requirements.txt`

### Ошибка: "Port 8000 is already in use"
**Решение:** 
- Остановите другой процесс на порту 8000
- Или измените порт: `set PORT=8001` (Windows) или `export PORT=8001` (Linux/Mac)

### Ошибка: "files/Шаблон.docx не найден"
**Решение:** Убедитесь что файл шаблона находится в папке `files/`

