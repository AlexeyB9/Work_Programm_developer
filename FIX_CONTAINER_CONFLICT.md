# 🔧 Исправление конфликта контейнера

## Проблема

Контейнер запущен напрямую через `docker run`, а не через `docker-compose`, поэтому возникает конфликт имен.

## Решение

### Вариант 1: Удалить старый контейнер и запустить через docker-compose (рекомендуется)

```bash
# 1. Остановите и удалите старый контейнер
docker stop guap
docker rm guap

# 2. Запустите через docker-compose
docker-compose up -d

# 3. Проверьте логи (правильная команда)
docker-compose logs -f guap
# или
docker logs -f guap
```

### Вариант 2: Использовать существующий контейнер

Если контейнер работает, можно просто проверить логи:

```bash
# Логи контейнера (правильная команда)
docker logs -f guap

# Или последние 100 строк
docker logs --tail=100 guap

# Проверка ошибок
docker logs guap | grep -i "error\|exception" | tail -20
```

## Проверка работы

```bash
# Статус
docker ps | grep guap

# Логи
docker logs --tail=50 guap

# Проверка переменных
docker exec guap env | grep -E "PPLX|TELEGRAM"

# Тест веб-сервера
curl http://localhost:8000
```

## Если нужно пересобрать

```bash
# 1. Остановите и удалите
docker stop guap
docker rm guap

# 2. Пересоберите через docker-compose
docker-compose build --no-cache

# 3. Запустите
docker-compose up -d

# 4. Логи
docker-compose logs -f
```

