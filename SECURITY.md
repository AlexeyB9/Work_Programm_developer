# 🔒 Безопасность - Управление секретами

## ⚠️ ВАЖНО: Секреты не должны быть в коде!

GitHub автоматически сканирует репозитории на наличие секретов (API ключи, токены и т.д.) и блокирует push, если они обнаружены.

## ✅ Правильный способ хранения секретов:

### 1. Perplexity API Key

**НЕ делайте так:**
```python
PPLX_API_KEY = "pplx-your-secret-key-here"  # ❌ НЕПРАВИЛЬНО!
```

**Делайте так:**
```python
PPLX_API_KEY = os.getenv("PPLX_API_KEY")  # ✅ ПРАВИЛЬНО!
```

**Настройка:**
- Локально: `export PPLX_API_KEY=your_key`
- На сервере: через панель управления хостинга (Railway, Render и т.д.)

### 2. Telegram Bot Token

**НЕ делайте так:**
```python
BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"  # ❌ НЕПРАВИЛЬНО!
```

**Делайте так:**
```python
BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")  # ✅ ПРАВИЛЬНО!
```

**Настройка:**
- Локально: `export TELEGRAM_BOT_TOKEN=your_token`
- На сервере: через панель управления хостинга

## 📝 Файлы с секретами:

- ✅ `config/env.example` - пример переменных окружения (без реальных ключей)
- ❌ `.env` - НЕ коммитьте! Добавлен в `.gitignore`
- ❌ Любые файлы с реальными ключами - НЕ коммитьте!

## 🔍 Проверка перед коммитом:

1. Убедитесь что нет захардкоженных ключей в коде
2. Проверьте что `.env` в `.gitignore`
3. Используйте `git status` чтобы увидеть что будет закоммичено
4. Если GitHub заблокировал push - удалите секрет из истории коммитов

## 🛠️ Если секрет уже был закоммичен:

1. **Удалите секрет из кода** (как показано выше)
2. **Сделайте новый коммит** с исправлением
3. **Если нужно удалить из истории:**
   ```bash
   git rebase -i HEAD~N  # где N - количество коммитов назад
   # или используйте git filter-branch / git filter-repo
   ```
4. **Смените секрет** (получите новый ключ/токен) - старый скомпрометирован

## 📚 Дополнительная информация:

- [GitHub Secret Scanning](https://docs.github.com/code-security/secret-scanning)
- [Working with secrets](https://docs.github.com/code-security/secret-scanning/working-with-secret-scanning-and-push-protection)

