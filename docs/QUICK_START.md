# 🚀 Быстрый старт развертывания

## Вариант 1: Railway (Рекомендуется) ⭐

1. Зарегистрируйтесь на https://railway.app (через GitHub)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. В настройках проекта добавьте переменные окружения:
   - `PPLX_API_KEY` = ваш ключ Perplexity API
   - `TELEGRAM_BOT_TOKEN` = токен бота (опционально)
5. Railway автоматически развернет проект
6. Получите публичный URL в настройках → "Settings" → "Generate Domain"

**Готово!** Ваше приложение доступно по публичному URL.

## Вариант 2: Render

1. Зарегистрируйтесь на https://render.com
2. Создайте "New Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python run_all.py`
   - **Environment:** Python 3
5. Добавьте переменные окружения:
   - `PPLX_API_KEY`
   - `TELEGRAM_BOT_TOKEN`
6. Нажмите "Create Web Service"

**Примечание:** На бесплатном тарифе сервис может "засыпать" после 15 минут неактивности.

## Вариант 3: PythonAnywhere

1. Зарегистрируйтесь на https://www.pythonanywhere.com
2. Загрузите проект через "Files" → "Upload a file" или Git
3. Создайте веб-приложение:
   - "Web" → "Add a new web app"
   - Выберите "Manual configuration" → Python 3.10
4. Настройте WSGI файл (замените `yourusername`):
   ```python
   import sys
   path = '/home/yourusername/Work_Programm_developer'
   if path not in sys.path:
       sys.path.insert(0, path)
   
   from api import app
   application = app
   ```
5. В консоли установите зависимости:
   ```bash
   pip3.10 install --user -r requirements.txt
   ```
6. Настройте переменные окружения в консоли:
   ```bash
   export PPLX_API_KEY="your_key"
   export TELEGRAM_BOT_TOKEN="your_token"
   ```
7. Запустите Telegram бота через "Tasks":
   ```bash
   cd ~/Work_Programm_developer
   python3.10 tgbot/bot.py
   ```
8. Перезагрузите веб-приложение

## Что нужно перед развертыванием:

✅ Файл `files/Шаблон.docx` должен быть в репозитории  
✅ Все зависимости указаны в `requirements.txt`  
✅ Переменные окружения настроены на сервере  

## Проверка после развертывания:

1. Откройте веб-интерфейс по URL
2. Попробуйте загрузить тестовый файл
3. Проверьте что Telegram бот отвечает (если настроен)

## Проблемы?

- Проверьте логи на сервере
- Убедитесь что переменные окружения установлены
- Проверьте что файл `files/Шаблон.docx` присутствует

Подробная инструкция в `docs/DEPLOY.md`

