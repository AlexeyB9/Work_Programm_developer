# 📚 Документация проекта

## Файлы документации:

- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт развертывания на популярных хостингах
- **[DEPLOY.md](DEPLOY.md)** - Подробная инструкция по развертыванию на различных платформах
- **[CHECKLIST.md](CHECKLIST.md)** - Чеклист перед развертыванием

## Структура проекта:

```
Work_Programm_developer/
├── api.py              # FastAPI веб-сервер
├── run_all.py          # Запуск всех сервисов
├── main.py             # CLI версия (опционально)
├── requirements.txt    # Зависимости Python
├── Procfile            # Для Railway/Heroku (должен быть в корне)
├── railway.json        # Конфигурация Railway (должен быть в корне)
├── .gitignore          # Игнорируемые файлы
├── wpd/                # Модули ядра приложения
├── tgbot/              # Telegram бот
├── files/              # Рабочие файлы
│   └── Шаблон.docx    # Шаблон (обязателен!)
├── config/             # Конфигурационные файлы
│   ├── Dockerfile      # Docker конфигурация
│   ├── runtime.txt     # Версия Python
│   └── env.example     # Пример переменных окружения
├── scripts/            # Утилиты для разработки
│   ├── cleanup.py      # Очистка временных файлов
│   ├── prepare_deploy.bat
│   └── prepare_deploy.sh
└── docs/               # Документация
    ├── QUICK_START.md
    ├── DEPLOY.md
    └── CHECKLIST.md
```

## Важные замечания:

1. **Procfile** и **railway.json** должны быть в корне проекта для корректной работы Railway/Heroku
2. **config/** содержит конфигурационные файлы, которые можно переместить при необходимости
3. **scripts/** содержит утилиты для разработки, не нужны на сервере
4. **docs/** содержит всю документацию по развертыванию

