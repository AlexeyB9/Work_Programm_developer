# 🐳 Запуск проекта через Docker на сервере

## Быстрый старт

### 1. Перейдите в папку проекта на сервере:
```bash
cd /path/to/Work_Programm_developer
```

### 2. Создайте файл `.env` с переменными окружения:
```bash
nano .env
```

Добавьте:
```env
PPLX_API_KEY=ваш_ключ_perplexity_api
TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
PORT=8000
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

### 3. Переместите Dockerfile в корень (если он в config/):
```bash
cp config/Dockerfile Dockerfile
```

### 4. Соберите Docker образ:
```bash
docker build -t guap-app .
```

### 5. Запустите контейнер:
```bash
docker run -d \
  --name guap \
  --restart unless-stopped \
  -p 8000:8000 \
  --env-file .env \
  guap-app
```

### 6. Проверьте что контейнер запущен:
```bash
docker ps
docker logs guap
```

## Управление контейнером

### Остановить:
```bash
docker stop guap
```

### Запустить:
```bash
docker start guap
```

### Перезапустить:
```bash
docker restart guap
```

### Посмотреть логи:
```bash
docker logs -f guap
```

### Удалить контейнер:
```bash
docker stop guap
docker rm guap
```

## Использование docker-compose (рекомендуется)

Создайте файл `docker-compose.yml`:

```yaml
version: '3.8'

services:
  app:
    build: .
    container_name: guap
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./files:/app/files
    environment:
      - PORT=8000
```

Запуск:
```bash
docker-compose up -d
```

Остановка:
```bash
docker-compose down
```

Логи:
```bash
docker-compose logs -f
```

## Проверка работы

После запуска откройте в браузере:
- Веб-интерфейс: `http://ваш_сервер:8000`
- Или если на localhost: `http://localhost:8000`

## Решение проблем

### Контейнер не запускается:
```bash
docker logs guap
```

### Проверка переменных окружения:
```bash
docker exec guap env | grep -E "PPLX|TELEGRAM|PORT"
```

### Вход в контейнер:
```bash
docker exec -it guap /bin/bash
```

### Пересборка после изменений:
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## Важные замечания

1. **Порт 8000** - убедитесь что он не занят другим приложением
2. **Файл шаблона** - должен быть в `files/Шаблон.docx`
3. **Переменные окружения** - обязательно настройте `.env` файл
4. **Файрвол** - откройте порт 8000 если нужен внешний доступ

